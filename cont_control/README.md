To run this environement you'll need PyBullet installed.
Install bullet using `pip` in your conda env.
Then install `pybullergym` using
```
git clone https://github.com/benelot/pybullet-gym.git
cd pybullet-gym
pip install -e .
```
